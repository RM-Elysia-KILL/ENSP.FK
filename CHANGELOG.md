# Changelog

## v1.0.5 — 2026-05-29

### 新增

- **自适应界面**：全面采用 Viewbox + UpOnly 等比缩放，窗口大小变化时 UI 实时自适应，最小 800px 不缩水
- **设备按钮分组折叠**：管控按钮分为「设备管控」「配置操作」两组，Expander 可折叠

### 修复

- `{helpers:Scale}` 在 `GridLength`/`DataGridLength` 属性上类型转换崩溃
- 4 个 XAML 文件 `xmlns:helpers` 声明位置错误导致编译失败
- 设备连接取消/异常时 session.State 不更新导致 UI 永久"等待就绪"
- 拓扑接口扩展块计数异常时槽位号分配错误
- 测试连接后 API 配置未持久化到磁盘

---

## v1.0.2 — 2026-05-14

### 新增

- **AI 配置验证**：生成配置后自动将结果返回 AI 进行二次审核，逐条对比需求与生成的 CLI 命令，检查参数精确性、遗漏项和语法正确性。验证结果在聊天面板实时展示，同时提供手动「验证配置」按钮
- **配置输出路径可配置**：设置页新增「配置命令存放路径」选项，用户可自定义导出目录。默认路径从工程源码目录改为 `%LocalAppData%\ENSP.ZD\配置输出\`，不再污染项目文件

### 修复

- **配置导出路径不当**：修复生成的 `.cfg` 文件默认写入工程源码目录的问题，改为写入用户数据目录

---

## v1.0.1 — 2026-05-13

### 新增

- **AI 生成耗时显示**：状态栏实时显示已用时间（"X 分 X 秒"），生成完成后在聊天消息中显示总耗时
- **API 配置持久化**：设置页的 Base URL、API Key、Model Name 自动保存到 `%LocalAppData%\ENSP.ZD\apiconfig.json`，重启无需重新输入
- **窗口标题动态更新**：加载拓扑文件后标题栏显示文件名

### 修复

#### Critical

- **StackOverflowException**：修复 `ApiConfig.Load()` 反序列化时无限递归导致进程崩溃，点击配置输出页面卡死
- **异常时 UI 永久禁用**：修复 AI 生成过程中发生未预期异常时 timer 泄漏、按钮永远无法点击的问题
- **显式接口崩溃**：修复 `MainWindow` 两个显式接口实现 `GetNavigation()` / `SetServiceProvider()` 直接抛 `NotImplementedException` 的潜在崩溃
- **页面数据过期**：修复 3 个页面（拓扑导入、任务需求、配置输出）切换后显示旧数据的问题，改为每次导航时从 Session 刷新
- **诊断页不刷新**：修复诊断页面只在首次加载时检查，切换回来不更新的问题

#### High

- **API 连通性测试代码重复**：`SettingsViewModel` 和 `AIConfigGenerator` 各有一份几乎相同的测试实现，合并为统一调用
- **AI 对话不自动滚动**：修复聊天面板新增消息后不自动滚到底部的问题
- **API Key 明文显示**：设置页的 API Key 输入框改为密码框，不再明文暴露
- **Telnet 异常静默吞没**：修复连接异常时无任何诊断输出的问题，增加 `Debug.WriteLine` 日志
- **ENSP 扫描页首次闪烁**：移除页面构造与导航各调用一次 `RefreshDevices()` 导致的重复加载

#### Medium

- **终端输出乱码**：修复输出超过 64KB 按字节截断导致顶部出现半行乱码，改为按换行边界截断
- **断开未连接设备显示误导**：区分"已断开"和"未连接"两种状态，不再对失败设备显示假消息
- **配置文件导出同名覆盖**：同名设备导出 `.cfg` 文件时自动追加 `_2` / `_3` 后缀，不再静默覆盖
- **添加需求后表单不清空**：修复添加需求后输入框保留旧值的问题，自动重置表单
- **一键连接/配置按钮可重复点击**：操作进行中禁用对应按钮，防止并发冲突
- **VBoxManage 静默失败**：修复 VirtualBox 命令行失败时无任何错误输出的问题

#### 清理

- 移除未使用的 `FormatAiResponseSummary` 死代码

---

## v0.1.0 — 首个可用版本

- 拓扑导入（.topo 文件解析）
- AI / 模板引擎双模式 CLI 配置生成
- 一键 Telnet 推送配置到 eNSP 设备
- ENSP 设备扫描与实时终端
- 深色/浅色主题切换
